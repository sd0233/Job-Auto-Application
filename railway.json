{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn dashboard.app:app --bind 0.0.0.0:8080 --log-level debug --timeout 120",
    "restartPolicyType": "ON_FAILURE"
  }
}
